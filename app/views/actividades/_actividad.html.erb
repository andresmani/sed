<tr id=<%= "actividad_#{actividad.id}" %>>

  <td><%= actividad.nombre %></td>
  <td><%= actividad.tema.unidad.programa.nombre %></td>
  <td><%= actividad.tema.unidad.nombre %></td>
  <td><%= actividad.tema.nombre %></td>
  <td><%= actividad.descripcion %></td>
  <td>
    <%= link_to 'Mostrar', tema_actividad_path(@tema, actividad), :remote => true, :class => 'btn btn-mini' %>
    <%= link_to 'Editar', edit_tema_actividad_path(@tema, actividad), :remote => true,
        :class => 'btn btn-mini' %>
    <%= link_to 'Eliminar', tema_actividad_path(@tema, actividad), :data => { :confirm => 'Esta Seguro?' },
        :method => :delete, :remote => true, :class => 'btn btn-mini btn-danger' %>
    </td>
</tr>
