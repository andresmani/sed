<%= stylesheet_link_tag "application", :media => "all" %>
<ul class="nav nav-tabs">
	<% if current_user %>
	<li>
	<%= link_to('Programas', programas_path)  %>
	</li>
	<li>
	<%= link_to('Materias', unidades_path) %>
	</li>
	<li>
	<%= link_to('Evaluaciones', evaluaciones_path) %>
	</li>
	<li>
	<%= link_to('Estudiantes', estudiantes_path) %>
	</li>
	<li> <div class="btn-group offset5" >
       <a class="btn btn-primary btn-mini dropdown-toggle" data-toggle="dropdown" href="#"><i><%= image_tag("user.png")%></i>-> <%= current_user.username %></a>
       <ul class="dropdown-menu">
        <li><%= link_to(logout_path) do %>
         <b>Salir </b> <i><%= image_tag("exit.png")%></i>
        </li>
       </ul>
      
           <% end %>
           
            <% end %>
  </div></li>
</ul>
